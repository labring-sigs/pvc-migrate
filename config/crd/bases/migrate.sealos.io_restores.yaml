---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: restores.migrate.sealos.io
spec:
  group: migrate.sealos.io
  names:
    kind: Restore
    listKind: RestoreList
    plural: restores
    shortNames:
    - rest
    singular: restore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              allowMounted:
                type: boolean
              createPVC:
                type: boolean
              createdBy:
                type: string
              deleteExtraneous:
                type: boolean
              destinationAccessMode:
                type: string
              destinationCapacity:
                type: string
              destinationNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              destinationPVC:
                description: ObjectReference identifies a Kubernetes object involved
                  in a workflow.
                properties:
                  apiVersion:
                    maxLength: 253
                    type: string
                  kind:
                    maxLength: 253
                    type: string
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                  namespace:
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  resourceVersion:
                    maxLength: 256
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                type: object
              destinationStorageClass:
                type: string
              name:
                maxLength: 253
                minLength: 1
                pattern: ^[A-Za-z0-9][A-Za-z0-9._-]*$
                type: string
              objectStoreProfile:
                description: |-
                  ObjectStoreProfile selects an administrator-owned cluster profile. The
                  profile supplies endpoint, bucket scope, and credentials.
                maxLength: 253
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                type: string
              path:
                type: string
              sessionNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              targetNode:
                type: string
              toolImage:
                type: string
            required:
            - destinationNamespace
            - destinationPVC
            - name
            - objectStoreProfile
            - sessionNamespace
            type: object
            x-kubernetes-validations:
            - message: workflow namespace must match metadata.namespace
              rule: self.destinationNamespace == self.sessionNamespace
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 8192
                      type: string
                    reason:
                      maxLength: 128
                      type: string
                    status:
                      type: string
                    type:
                      maxLength: 64
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
              failureReason:
                maxLength: 8192
                type: string
              history:
                items:
                  properties:
                    message:
                      maxLength: 8192
                      type: string
                    phase:
                      description: WorkflowPhase identifies one durable workflow state.
                      enum:
                      - Planned
                      - Reserving
                      - Reserved
                      - WarmCopying
                      - WarmCopied
                      - Pausing
                      - Paused
                      - FinalSyncing
                      - FinalSynced
                      - Activating
                      - Activated
                      - Resuming
                      - Completed
                      - Aborting
                      - Aborted
                      - RollingBack
                      - RolledBack
                      - Renaming
                      - Moving
                      - Failed
                      type: string
                    time:
                      format: date-time
                      type: string
                  required:
                  - phase
                  - time
                  type: object
                maxItems: 256
                type: array
              message:
                maxLength: 8192
                type: string
              objectStoreCredentialsSecretUID:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
              objectStoreProfileGeneration:
                format: int64
                type: integer
              objectStoreProfileUID:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
              objectStoreServiceAccountFingerprint:
                type: string
              objectStoreServiceAccountUID:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              resumeFrom:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              startedAt:
                format: date-time
                type: string
              updatedAt:
                format: date-time
                type: string
            required:
            - phase
            - startedAt
            - updatedAt
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
