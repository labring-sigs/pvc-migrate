---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: backups.migrate.sealos.io
spec:
  group: migrate.sealos.io
  names:
    kind: Backup
    listKind: BackupList
    plural: backups
    shortNames:
    - pback
    singular: backup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              createdBy:
                type: string
              deleteExtraneous:
                type: boolean
              name:
                maxLength: 253
                minLength: 1
                pattern: ^[A-Za-z0-9][A-Za-z0-9._-]*$
                type: string
              objectStoreProfile:
                description: |-
                  ObjectStoreProfile selects an administrator-owned cluster profile. The
                  profile supplies endpoint, bucket scope, and credentials.
                maxLength: 253
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                type: string
              online:
                type: boolean
              openebsLvmEnableShared:
                type: boolean
              path:
                type: string
              sessionNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              sourceNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              sourcePV:
                description: ObjectReference identifies a Kubernetes object involved
                  in a workflow.
                properties:
                  apiVersion:
                    maxLength: 253
                    type: string
                  kind:
                    maxLength: 253
                    type: string
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                  namespace:
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  resourceVersion:
                    maxLength: 256
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                type: object
              sourcePVC:
                description: ObjectReference identifies a Kubernetes object involved
                  in a workflow.
                properties:
                  apiVersion:
                    maxLength: 253
                    type: string
                  kind:
                    maxLength: 253
                    type: string
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                  namespace:
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  resourceVersion:
                    maxLength: 256
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                type: object
              toolImage:
                type: string
            required:
            - name
            - objectStoreProfile
            - sessionNamespace
            - sourceNamespace
            - sourcePV
            - sourcePVC
            type: object
            x-kubernetes-validations:
            - message: workflow namespace must match metadata.namespace
              rule: self.sourceNamespace == self.sessionNamespace
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 8192
                      type: string
                    reason:
                      maxLength: 128
                      type: string
                    status:
                      type: string
                    type:
                      maxLength: 64
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
              failureReason:
                maxLength: 8192
                type: string
              history:
                items:
                  properties:
                    message:
                      maxLength: 8192
                      type: string
                    phase:
                      description: WorkflowPhase identifies one durable workflow state.
                      enum:
                      - Planned
                      - Reserving
                      - Reserved
                      - WarmCopying
                      - WarmCopied
                      - Pausing
                      - Paused
                      - FinalSyncing
                      - FinalSynced
                      - Activating
                      - Activated
                      - Resuming
                      - Completed
                      - Aborting
                      - Aborted
                      - RollingBack
                      - RolledBack
                      - Renaming
                      - Moving
                      - Failed
                      type: string
                    time:
                      format: date-time
                      type: string
                  required:
                  - phase
                  - time
                  type: object
                maxItems: 256
                type: array
              message:
                maxLength: 8192
                type: string
              objectStoreCredentialsSecretUID:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
              objectStoreProfileGeneration:
                format: int64
                type: integer
              objectStoreProfileUID:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
              objectStoreServiceAccountFingerprint:
                type: string
              objectStoreServiceAccountUID:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
              observedGeneration:
                format: int64
                type: integer
              openebsLvmSharedMounts:
                items:
                  properties:
                    lvmVolume:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    previousShared:
                      type: string
                    previousSharedSet:
                      type: boolean
                    sourcePV:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                  required:
                  - lvmVolume
                  - sourcePV
                  type: object
                type: array
              phase:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              resumeFrom:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              startedAt:
                format: date-time
                type: string
              updatedAt:
                format: date-time
                type: string
            required:
            - phase
            - startedAt
            - updatedAt
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: copies.migrate.sealos.io
spec:
  group: migrate.sealos.io
  names:
    kind: Copy
    listKind: CopyList
    plural: copies
    shortNames:
    - pcopy
    singular: copy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              createdBy:
                type: string
              deleteExtraneous:
                type: boolean
              destinationNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              online:
                type: boolean
              sessionNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              skipSourceUsageCheck:
                type: boolean
              sourceNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              sourceNode:
                type: string
              strategies:
                items:
                  type: string
                maxItems: 32
                type: array
              targetNode:
                type: string
              temporaryNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              toolImage:
                type: string
              volumes:
                items:
                  description: |-
                    VolumeSpec is planning output required to resume a PVC transfer. It is an
                    API-owned type with only the fields needed by transfer workflows.
                  properties:
                    accessModes:
                      items:
                        type: string
                      type: array
                    capacity:
                      type: string
                    concurrentConsumers:
                      type: integer
                    destinationPVC:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    sourceCapacity:
                      type: string
                    sourcePV:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    sourcePVC:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    sourcePVCMetadata:
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          type: object
                        labels:
                          additionalProperties:
                            type: string
                          type: object
                        ownerReferences:
                          items:
                            description: |-
                              OwnerReference contains enough information to let you identify an owning
                              object. An owning object must be in the same namespace as the dependent, or
                              be cluster-scoped, so there is no namespace field.
                            properties:
                              apiVersion:
                                description: API version of the referent.
                                type: string
                              blockOwnerDeletion:
                                description: |-
                                  If true, AND if the owner has the "foregroundDeletion" finalizer, then
                                  the owner cannot be deleted from the key-value store until this
                                  reference is removed.
                                  See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion
                                  for how the garbage collector interacts with this field and enforces the foreground deletion.
                                  Defaults to false.
                                  To set this field, a user needs "delete" permission of the owner,
                                  otherwise 422 (Unprocessable Entity) will be returned.
                                type: boolean
                              controller:
                                description: If true, this reference points to the
                                  managing controller.
                                type: boolean
                              kind:
                                description: |-
                                  Kind of the referent.
                                  More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                                type: string
                              name:
                                description: |-
                                  Name of the referent.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
                                type: string
                              uid:
                                description: |-
                                  UID of the referent.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
                                type: string
                            required:
                            - apiVersion
                            - kind
                            - name
                            - uid
                            type: object
                            x-kubernetes-map-type: atomic
                          type: array
                      type: object
                    sourcePVCSpec:
                      description: |-
                        PersistentVolumeClaimSpec describes the common attributes of storage devices
                        and allows a Source for provider-specific attributes
                      properties:
                        accessModes:
                          description: |-
                            accessModes contains the desired access modes the volume should have.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        dataSource:
                          description: |-
                            dataSource field can be used to specify either:
                            * An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)
                            * An existing PVC (PersistentVolumeClaim)
                            If the provisioner or an external controller can support the specified data source,
                            it will create a new volume based on the contents of the specified data source.
                            When the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,
                            and dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.
                            If the namespace is specified, then dataSourceRef will not be copied to dataSource.
                          properties:
                            apiGroup:
                              description: |-
                                APIGroup is the group for the resource being referenced.
                                If APIGroup is not specified, the specified Kind must be in the core API group.
                                For any other third-party types, APIGroup is required.
                              type: string
                            kind:
                              description: Kind is the type of resource being referenced
                              type: string
                            name:
                              description: Name is the name of resource being referenced
                              type: string
                          required:
                          - kind
                          - name
                          type: object
                          x-kubernetes-map-type: atomic
                        dataSourceRef:
                          description: |-
                            dataSourceRef specifies the object from which to populate the volume with data, if a non-empty
                            volume is desired. This may be any object from a non-empty API group (non
                            core object) or a PersistentVolumeClaim object.
                            When this field is specified, volume binding will only succeed if the type of
                            the specified object matches some installed volume populator or dynamic
                            provisioner.
                            This field will replace the functionality of the dataSource field and as such
                            if both fields are non-empty, they must have the same value. For backwards
                            compatibility, when namespace isn't specified in dataSourceRef,
                            both fields (dataSource and dataSourceRef) will be set to the same
                            value automatically if one of them is empty and the other is non-empty.
                            When namespace is specified in dataSourceRef,
                            dataSource isn't set to the same value and must be empty.
                            There are three important differences between dataSource and dataSourceRef:
                            * While dataSource only allows two specific types of objects, dataSourceRef
                              allows any non-core object, as well as PersistentVolumeClaim objects.
                            * While dataSource ignores disallowed values (dropping them), dataSourceRef
                              preserves all values, and generates an error if a disallowed value is
                              specified.
                            * While dataSource only allows local objects, dataSourceRef allows objects
                              in any namespaces.
                            (Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.
                            (Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                          properties:
                            apiGroup:
                              description: |-
                                APIGroup is the group for the resource being referenced.
                                If APIGroup is not specified, the specified Kind must be in the core API group.
                                For any other third-party types, APIGroup is required.
                              type: string
                            kind:
                              description: Kind is the type of resource being referenced
                              type: string
                            name:
                              description: Name is the name of resource being referenced
                              type: string
                            namespace:
                              description: |-
                                Namespace is the namespace of resource being referenced
                                Note that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.
                                (Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                              type: string
                          required:
                          - kind
                          - name
                          type: object
                        resources:
                          description: |-
                            resources represents the minimum resources the volume should have.
                            Users are allowed to specify resource requirements
                            that are lower than previous value but must still be higher than capacity recorded in the
                            status field of the claim.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources
                          properties:
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                        selector:
                          description: selector is a label query over volumes to consider
                            for binding.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        storageClassName:
                          description: |-
                            storageClassName is the name of the StorageClass required by the claim.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1
                          type: string
                        volumeAttributesClassName:
                          description: |-
                            volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.
                            If specified, the CSI driver will create or update the volume with the attributes defined
                            in the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,
                            it can be changed after the claim is created. An empty string or nil value indicates that no
                            VolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,
                            this field can be reset to its previous value (including nil) to cancel the modification.
                            If the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be
                            set to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource
                            exists.
                            More info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/
                          type: string
                        volumeMode:
                          description: |-
                            volumeMode defines what type of volume is required by the claim.
                            Value of Filesystem is implied when not included in claim spec.
                          type: string
                        volumeName:
                          description: volumeName is the binding reference to the
                            PersistentVolume backing this claim.
                          type: string
                      type: object
                    sourceReclaimPolicy:
                      description: PersistentVolumeReclaimPolicy describes a policy
                        for end-of-life maintenance of persistent volumes.
                      type: string
                    sourceUsageKnown:
                      type: boolean
                    sourceUsedBytes:
                      format: int64
                      type: integer
                    storageClass:
                      type: string
                    transferScope:
                      properties:
                        destinationPath:
                          maxLength: 1024
                          type: string
                        sourcePath:
                          maxLength: 1024
                          type: string
                      required:
                      - destinationPath
                      - sourcePath
                      type: object
                    volumeMode:
                      description: PersistentVolumeMode describes how a volume is
                        intended to be consumed, either Block or Filesystem.
                      type: string
                  required:
                  - accessModes
                  - capacity
                  - destinationPVC
                  - sourceCapacity
                  - sourcePV
                  - sourcePVC
                  - storageClass
                  - volumeMode
                  type: object
                maxItems: 1024
                type: array
            required:
            - destinationNamespace
            - sessionNamespace
            - sourceNamespace
            - temporaryNamespace
            type: object
            x-kubernetes-validations:
            - message: workflow namespaces must match metadata.namespace
              rule: self.sourceNamespace == self.sessionNamespace && self.destinationNamespace
                == self.sessionNamespace && self.temporaryNamespace == self.sessionNamespace
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 8192
                      type: string
                    reason:
                      maxLength: 128
                      type: string
                    status:
                      type: string
                    type:
                      maxLength: 64
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
              failureReason:
                maxLength: 8192
                type: string
              history:
                items:
                  properties:
                    message:
                      maxLength: 8192
                      type: string
                    phase:
                      description: WorkflowPhase identifies one durable workflow state.
                      enum:
                      - Planned
                      - Reserving
                      - Reserved
                      - WarmCopying
                      - WarmCopied
                      - Pausing
                      - Paused
                      - FinalSyncing
                      - FinalSynced
                      - Activating
                      - Activated
                      - Resuming
                      - Completed
                      - Aborting
                      - Aborted
                      - RollingBack
                      - RolledBack
                      - Renaming
                      - Moving
                      - Failed
                      type: string
                    time:
                      format: date-time
                      type: string
                  required:
                  - phase
                  - time
                  type: object
                maxItems: 256
                type: array
              message:
                maxLength: 8192
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              resumeFrom:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              startedAt:
                format: date-time
                type: string
              updatedAt:
                format: date-time
                type: string
              volumes:
                items:
                  description: |-
                    CopyVolumeStatus reports reservation and copy progress. PVC activation is
                    owned by Migration/PodMigration and is intentionally absent here.
                  properties:
                    destinationPV:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    destinationPVC:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    destinationReclaimPolicy:
                      description: PersistentVolumeReclaimPolicy describes a policy
                        for end-of-life maintenance of persistent volumes.
                      type: string
                    reserved:
                      type: boolean
                    sourcePVCName:
                      type: string
                    sync:
                      description: CopySyncStatus tracks the warm-copy operation owned
                        by Copy.
                      properties:
                        attempts:
                          type: integer
                        bytesCopied:
                          format: int64
                          type: integer
                        lastError:
                          maxLength: 8192
                          type: string
                        warmCompletedAt:
                          format: date-time
                          type: string
                      required:
                      - attempts
                      type: object
                  required:
                  - sourcePVCName
                  - sync
                  type: object
                type: array
            required:
            - phase
            - startedAt
            - updatedAt
            - volumes
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: migrations.migrate.sealos.io
spec:
  group: migrate.sealos.io
  names:
    kind: Migration
    listKind: MigrationList
    plural: migrations
    shortNames:
    - mig
    singular: migration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Migration is the controller-backed durable offline PVC migration resource.
          Its API contract is independent from the internal session state machine;
          the CRD session store performs the explicit adapter conversion.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MigrationSpec is an offline PVC migration. It has no workload
              controls.
            properties:
              createdBy:
                type: string
              deleteExtraneous:
                type: boolean
              destinationNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              sessionNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              skipSourceUsageCheck:
                type: boolean
              sourceNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              sourceNode:
                type: string
              strategies:
                items:
                  type: string
                maxItems: 32
                type: array
              targetNode:
                type: string
              temporaryNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              toolImage:
                type: string
              verifyChecksum:
                type: boolean
              volumes:
                items:
                  description: |-
                    VolumeSpec is planning output required to resume a PVC transfer. It is an
                    API-owned type with only the fields needed by transfer workflows.
                  properties:
                    accessModes:
                      items:
                        type: string
                      type: array
                    capacity:
                      type: string
                    concurrentConsumers:
                      type: integer
                    destinationPVC:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    sourceCapacity:
                      type: string
                    sourcePV:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    sourcePVC:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    sourcePVCMetadata:
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          type: object
                        labels:
                          additionalProperties:
                            type: string
                          type: object
                        ownerReferences:
                          items:
                            description: |-
                              OwnerReference contains enough information to let you identify an owning
                              object. An owning object must be in the same namespace as the dependent, or
                              be cluster-scoped, so there is no namespace field.
                            properties:
                              apiVersion:
                                description: API version of the referent.
                                type: string
                              blockOwnerDeletion:
                                description: |-
                                  If true, AND if the owner has the "foregroundDeletion" finalizer, then
                                  the owner cannot be deleted from the key-value store until this
                                  reference is removed.
                                  See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion
                                  for how the garbage collector interacts with this field and enforces the foreground deletion.
                                  Defaults to false.
                                  To set this field, a user needs "delete" permission of the owner,
                                  otherwise 422 (Unprocessable Entity) will be returned.
                                type: boolean
                              controller:
                                description: If true, this reference points to the
                                  managing controller.
                                type: boolean
                              kind:
                                description: |-
                                  Kind of the referent.
                                  More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                                type: string
                              name:
                                description: |-
                                  Name of the referent.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
                                type: string
                              uid:
                                description: |-
                                  UID of the referent.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
                                type: string
                            required:
                            - apiVersion
                            - kind
                            - name
                            - uid
                            type: object
                            x-kubernetes-map-type: atomic
                          type: array
                      type: object
                    sourcePVCSpec:
                      description: |-
                        PersistentVolumeClaimSpec describes the common attributes of storage devices
                        and allows a Source for provider-specific attributes
                      properties:
                        accessModes:
                          description: |-
                            accessModes contains the desired access modes the volume should have.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        dataSource:
                          description: |-
                            dataSource field can be used to specify either:
                            * An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)
                            * An existing PVC (PersistentVolumeClaim)
                            If the provisioner or an external controller can support the specified data source,
                            it will create a new volume based on the contents of the specified data source.
                            When the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,
                            and dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.
                            If the namespace is specified, then dataSourceRef will not be copied to dataSource.
                          properties:
                            apiGroup:
                              description: |-
                                APIGroup is the group for the resource being referenced.
                                If APIGroup is not specified, the specified Kind must be in the core API group.
                                For any other third-party types, APIGroup is required.
                              type: string
                            kind:
                              description: Kind is the type of resource being referenced
                              type: string
                            name:
                              description: Name is the name of resource being referenced
                              type: string
                          required:
                          - kind
                          - name
                          type: object
                          x-kubernetes-map-type: atomic
                        dataSourceRef:
                          description: |-
                            dataSourceRef specifies the object from which to populate the volume with data, if a non-empty
                            volume is desired. This may be any object from a non-empty API group (non
                            core object) or a PersistentVolumeClaim object.
                            When this field is specified, volume binding will only succeed if the type of
                            the specified object matches some installed volume populator or dynamic
                            provisioner.
                            This field will replace the functionality of the dataSource field and as such
                            if both fields are non-empty, they must have the same value. For backwards
                            compatibility, when namespace isn't specified in dataSourceRef,
                            both fields (dataSource and dataSourceRef) will be set to the same
                            value automatically if one of them is empty and the other is non-empty.
                            When namespace is specified in dataSourceRef,
                            dataSource isn't set to the same value and must be empty.
                            There are three important differences between dataSource and dataSourceRef:
                            * While dataSource only allows two specific types of objects, dataSourceRef
                              allows any non-core object, as well as PersistentVolumeClaim objects.
                            * While dataSource ignores disallowed values (dropping them), dataSourceRef
                              preserves all values, and generates an error if a disallowed value is
                              specified.
                            * While dataSource only allows local objects, dataSourceRef allows objects
                              in any namespaces.
                            (Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.
                            (Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                          properties:
                            apiGroup:
                              description: |-
                                APIGroup is the group for the resource being referenced.
                                If APIGroup is not specified, the specified Kind must be in the core API group.
                                For any other third-party types, APIGroup is required.
                              type: string
                            kind:
                              description: Kind is the type of resource being referenced
                              type: string
                            name:
                              description: Name is the name of resource being referenced
                              type: string
                            namespace:
                              description: |-
                                Namespace is the namespace of resource being referenced
                                Note that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.
                                (Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                              type: string
                          required:
                          - kind
                          - name
                          type: object
                        resources:
                          description: |-
                            resources represents the minimum resources the volume should have.
                            Users are allowed to specify resource requirements
                            that are lower than previous value but must still be higher than capacity recorded in the
                            status field of the claim.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources
                          properties:
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                        selector:
                          description: selector is a label query over volumes to consider
                            for binding.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        storageClassName:
                          description: |-
                            storageClassName is the name of the StorageClass required by the claim.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1
                          type: string
                        volumeAttributesClassName:
                          description: |-
                            volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.
                            If specified, the CSI driver will create or update the volume with the attributes defined
                            in the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,
                            it can be changed after the claim is created. An empty string or nil value indicates that no
                            VolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,
                            this field can be reset to its previous value (including nil) to cancel the modification.
                            If the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be
                            set to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource
                            exists.
                            More info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/
                          type: string
                        volumeMode:
                          description: |-
                            volumeMode defines what type of volume is required by the claim.
                            Value of Filesystem is implied when not included in claim spec.
                          type: string
                        volumeName:
                          description: volumeName is the binding reference to the
                            PersistentVolume backing this claim.
                          type: string
                      type: object
                    sourceReclaimPolicy:
                      description: PersistentVolumeReclaimPolicy describes a policy
                        for end-of-life maintenance of persistent volumes.
                      type: string
                    sourceUsageKnown:
                      type: boolean
                    sourceUsedBytes:
                      format: int64
                      type: integer
                    storageClass:
                      type: string
                    transferScope:
                      properties:
                        destinationPath:
                          maxLength: 1024
                          type: string
                        sourcePath:
                          maxLength: 1024
                          type: string
                      required:
                      - destinationPath
                      - sourcePath
                      type: object
                    volumeMode:
                      description: PersistentVolumeMode describes how a volume is
                        intended to be consumed, either Block or Filesystem.
                      type: string
                  required:
                  - accessModes
                  - capacity
                  - destinationPVC
                  - sourceCapacity
                  - sourcePV
                  - sourcePVC
                  - storageClass
                  - volumeMode
                  type: object
                maxItems: 1024
                type: array
            required:
            - destinationNamespace
            - sessionNamespace
            - sourceNamespace
            - temporaryNamespace
            type: object
            x-kubernetes-validations:
            - message: workflow namespaces must match metadata.namespace
              rule: self.sourceNamespace == self.sessionNamespace && self.destinationNamespace
                == self.sessionNamespace && self.temporaryNamespace == self.sessionNamespace
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 8192
                      type: string
                    reason:
                      maxLength: 128
                      type: string
                    status:
                      type: string
                    type:
                      maxLength: 64
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
              failureReason:
                maxLength: 8192
                type: string
              history:
                items:
                  properties:
                    message:
                      maxLength: 8192
                      type: string
                    phase:
                      description: WorkflowPhase identifies one durable workflow state.
                      enum:
                      - Planned
                      - Reserving
                      - Reserved
                      - WarmCopying
                      - WarmCopied
                      - Pausing
                      - Paused
                      - FinalSyncing
                      - FinalSynced
                      - Activating
                      - Activated
                      - Resuming
                      - Completed
                      - Aborting
                      - Aborted
                      - RollingBack
                      - RolledBack
                      - Renaming
                      - Moving
                      - Failed
                      type: string
                    time:
                      format: date-time
                      type: string
                  required:
                  - phase
                  - time
                  type: object
                maxItems: 256
                type: array
              message:
                maxLength: 8192
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              resumeFrom:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              startedAt:
                format: date-time
                type: string
              updatedAt:
                format: date-time
                type: string
              volumes:
                items:
                  description: |-
                    MigrationVolumeStatus is the durable checkpoint for an offline migration
                    volume. It intentionally excludes workload-only progress and OpenEBS
                    shared-mount state.
                  properties:
                    activation:
                      properties:
                        activatedAt:
                          format: date-time
                          type: string
                        activePVC:
                          description: ObjectReference identifies a Kubernetes object
                            involved in a workflow.
                          properties:
                            apiVersion:
                              maxLength: 253
                              type: string
                            kind:
                              maxLength: 253
                              type: string
                            name:
                              maxLength: 253
                              minLength: 1
                              pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                              type: string
                            namespace:
                              maxLength: 63
                              pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                              type: string
                            resourceVersion:
                              maxLength: 256
                              type: string
                            uid:
                              description: |-
                                UID is a type that holds unique ID values, including UUIDs.  Because we
                                don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                                intent and helps make sure that UIDs and names do not get conflated.
                              type: string
                          required:
                          - name
                          type: object
                        destinationReserved:
                          type: boolean
                        rolledBackAt:
                          format: date-time
                          type: string
                        sourcePVCDeleted:
                          type: boolean
                        temporaryPVCDeleted:
                          type: boolean
                      type: object
                    destinationPV:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    destinationPVC:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    destinationReclaimPolicy:
                      description: PersistentVolumeReclaimPolicy describes a policy
                        for end-of-life maintenance of persistent volumes.
                      type: string
                    reserved:
                      type: boolean
                    sourcePVCName:
                      type: string
                    sync:
                      description: |-
                        MigrationSyncStatus contains final-copy checkpoints. Offline Migration has
                        no warm-copy phase, so warmCompletedAt is intentionally absent.
                      properties:
                        attempts:
                          type: integer
                        bytesCopied:
                          format: int64
                          type: integer
                        checksumVerified:
                          type: boolean
                        finalCompletedAt:
                          format: date-time
                          type: string
                        lastError:
                          maxLength: 8192
                          type: string
                      required:
                      - attempts
                      type: object
                  required:
                  - activation
                  - sourcePVCName
                  - sync
                  type: object
                type: array
            required:
            - phase
            - startedAt
            - updatedAt
            - volumes
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: moves.migrate.sealos.io
spec:
  group: migrate.sealos.io
  names:
    kind: Move
    listKind: MoveList
    plural: moves
    shortNames:
    - pmove
    singular: move
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              createdBy:
                type: string
              destinationNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              destinationPVC:
                description: ObjectReference identifies a Kubernetes object involved
                  in a workflow.
                properties:
                  apiVersion:
                    maxLength: 253
                    type: string
                  kind:
                    maxLength: 253
                    type: string
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                  namespace:
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  resourceVersion:
                    maxLength: 256
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                type: object
              sessionNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              sourceNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              sourcePV:
                description: ObjectReference identifies a Kubernetes object involved
                  in a workflow.
                properties:
                  apiVersion:
                    maxLength: 253
                    type: string
                  kind:
                    maxLength: 253
                    type: string
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                  namespace:
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  resourceVersion:
                    maxLength: 256
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                type: object
              sourcePVC:
                description: ObjectReference identifies a Kubernetes object involved
                  in a workflow.
                properties:
                  apiVersion:
                    maxLength: 253
                    type: string
                  kind:
                    maxLength: 253
                    type: string
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                  namespace:
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  resourceVersion:
                    maxLength: 256
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                type: object
              sourceTemplate:
                properties:
                  metadata:
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                      ownerReferences:
                        items:
                          description: |-
                            OwnerReference contains enough information to let you identify an owning
                            object. An owning object must be in the same namespace as the dependent, or
                            be cluster-scoped, so there is no namespace field.
                          properties:
                            apiVersion:
                              description: API version of the referent.
                              type: string
                            blockOwnerDeletion:
                              description: |-
                                If true, AND if the owner has the "foregroundDeletion" finalizer, then
                                the owner cannot be deleted from the key-value store until this
                                reference is removed.
                                See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion
                                for how the garbage collector interacts with this field and enforces the foreground deletion.
                                Defaults to false.
                                To set this field, a user needs "delete" permission of the owner,
                                otherwise 422 (Unprocessable Entity) will be returned.
                              type: boolean
                            controller:
                              description: If true, this reference points to the managing
                                controller.
                              type: boolean
                            kind:
                              description: |-
                                Kind of the referent.
                                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                              type: string
                            name:
                              description: |-
                                Name of the referent.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
                              type: string
                            uid:
                              description: |-
                                UID of the referent.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
                              type: string
                          required:
                          - apiVersion
                          - kind
                          - name
                          - uid
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                    type: object
                  reclaimPolicy:
                    description: PersistentVolumeReclaimPolicy describes a policy
                      for end-of-life maintenance of persistent volumes.
                    type: string
                  spec:
                    description: |-
                      PersistentVolumeClaimSpec describes the common attributes of storage devices
                      and allows a Source for provider-specific attributes
                    properties:
                      accessModes:
                        description: |-
                          accessModes contains the desired access modes the volume should have.
                          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: atomic
                      dataSource:
                        description: |-
                          dataSource field can be used to specify either:
                          * An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)
                          * An existing PVC (PersistentVolumeClaim)
                          If the provisioner or an external controller can support the specified data source,
                          it will create a new volume based on the contents of the specified data source.
                          When the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,
                          and dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.
                          If the namespace is specified, then dataSourceRef will not be copied to dataSource.
                        properties:
                          apiGroup:
                            description: |-
                              APIGroup is the group for the resource being referenced.
                              If APIGroup is not specified, the specified Kind must be in the core API group.
                              For any other third-party types, APIGroup is required.
                            type: string
                          kind:
                            description: Kind is the type of resource being referenced
                            type: string
                          name:
                            description: Name is the name of resource being referenced
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                        x-kubernetes-map-type: atomic
                      dataSourceRef:
                        description: |-
                          dataSourceRef specifies the object from which to populate the volume with data, if a non-empty
                          volume is desired. This may be any object from a non-empty API group (non
                          core object) or a PersistentVolumeClaim object.
                          When this field is specified, volume binding will only succeed if the type of
                          the specified object matches some installed volume populator or dynamic
                          provisioner.
                          This field will replace the functionality of the dataSource field and as such
                          if both fields are non-empty, they must have the same value. For backwards
                          compatibility, when namespace isn't specified in dataSourceRef,
                          both fields (dataSource and dataSourceRef) will be set to the same
                          value automatically if one of them is empty and the other is non-empty.
                          When namespace is specified in dataSourceRef,
                          dataSource isn't set to the same value and must be empty.
                          There are three important differences between dataSource and dataSourceRef:
                          * While dataSource only allows two specific types of objects, dataSourceRef
                            allows any non-core object, as well as PersistentVolumeClaim objects.
                          * While dataSource ignores disallowed values (dropping them), dataSourceRef
                            preserves all values, and generates an error if a disallowed value is
                            specified.
                          * While dataSource only allows local objects, dataSourceRef allows objects
                            in any namespaces.
                          (Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.
                          (Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                        properties:
                          apiGroup:
                            description: |-
                              APIGroup is the group for the resource being referenced.
                              If APIGroup is not specified, the specified Kind must be in the core API group.
                              For any other third-party types, APIGroup is required.
                            type: string
                          kind:
                            description: Kind is the type of resource being referenced
                            type: string
                          name:
                            description: Name is the name of resource being referenced
                            type: string
                          namespace:
                            description: |-
                              Namespace is the namespace of resource being referenced
                              Note that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.
                              (Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      resources:
                        description: |-
                          resources represents the minimum resources the volume should have.
                          Users are allowed to specify resource requirements
                          that are lower than previous value but must still be higher than capacity recorded in the
                          status field of the claim.
                          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources
                        properties:
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      selector:
                        description: selector is a label query over volumes to consider
                          for binding.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                      storageClassName:
                        description: |-
                          storageClassName is the name of the StorageClass required by the claim.
                          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1
                        type: string
                      volumeAttributesClassName:
                        description: |-
                          volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.
                          If specified, the CSI driver will create or update the volume with the attributes defined
                          in the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,
                          it can be changed after the claim is created. An empty string or nil value indicates that no
                          VolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,
                          this field can be reset to its previous value (including nil) to cancel the modification.
                          If the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be
                          set to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource
                          exists.
                          More info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/
                        type: string
                      volumeMode:
                        description: |-
                          volumeMode defines what type of volume is required by the claim.
                          Value of Filesystem is implied when not included in claim spec.
                        type: string
                      volumeName:
                        description: volumeName is the binding reference to the PersistentVolume
                          backing this claim.
                        type: string
                    type: object
                required:
                - spec
                type: object
            required:
            - destinationNamespace
            - destinationPVC
            - sessionNamespace
            - sourceNamespace
            - sourcePV
            - sourcePVC
            - sourceTemplate
            type: object
            x-kubernetes-validations:
            - message: Move CRs must stay within one namespace; cross-namespace moves
                use session mode
              rule: self.sourceNamespace == self.sessionNamespace && self.destinationNamespace
                == self.sessionNamespace
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 8192
                      type: string
                    reason:
                      maxLength: 128
                      type: string
                    status:
                      type: string
                    type:
                      maxLength: 64
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
              failureReason:
                maxLength: 8192
                type: string
              history:
                items:
                  properties:
                    message:
                      maxLength: 8192
                      type: string
                    phase:
                      description: WorkflowPhase identifies one durable workflow state.
                      enum:
                      - Planned
                      - Reserving
                      - Reserved
                      - WarmCopying
                      - WarmCopied
                      - Pausing
                      - Paused
                      - FinalSyncing
                      - FinalSynced
                      - Activating
                      - Activated
                      - Resuming
                      - Completed
                      - Aborting
                      - Aborted
                      - RollingBack
                      - RolledBack
                      - Renaming
                      - Moving
                      - Failed
                      type: string
                    time:
                      format: date-time
                      type: string
                  required:
                  - phase
                  - time
                  type: object
                maxItems: 256
                type: array
              message:
                maxLength: 8192
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              resumeFrom:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              startedAt:
                format: date-time
                type: string
              updatedAt:
                format: date-time
                type: string
              volumes:
                items:
                  description: |-
                    PVCIdentityVolumeStatus is the checkpoint for Rename and Move. Those
                    operations change PVC identity and never run a data-copy phase.
                  properties:
                    activation:
                      description: |-
                        PVCIdentityActivationStatus is the checkpoint needed to roll back a
                        Rename or Move. Temporary-volume cleanup fields do not apply to identity
                        operations.
                      properties:
                        activatedAt:
                          format: date-time
                          type: string
                        activePVC:
                          description: ObjectReference identifies a Kubernetes object
                            involved in a workflow.
                          properties:
                            apiVersion:
                              maxLength: 253
                              type: string
                            kind:
                              maxLength: 253
                              type: string
                            name:
                              maxLength: 253
                              minLength: 1
                              pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                              type: string
                            namespace:
                              maxLength: 63
                              pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                              type: string
                            resourceVersion:
                              maxLength: 256
                              type: string
                            uid:
                              description: |-
                                UID is a type that holds unique ID values, including UUIDs.  Because we
                                don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                                intent and helps make sure that UIDs and names do not get conflated.
                              type: string
                          required:
                          - name
                          type: object
                        rolledBackAt:
                          format: date-time
                          type: string
                      type: object
                    sourcePVCName:
                      type: string
                  required:
                  - activation
                  - sourcePVCName
                  type: object
                type: array
            required:
            - phase
            - startedAt
            - updatedAt
            - volumes
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: objectstoreprofiles.migrate.sealos.io
spec:
  group: migrate.sealos.io
  names:
    kind: ObjectStoreProfile
    listKind: ObjectStoreProfileList
    plural: objectstoreprofiles
    shortNames:
    - osp
    singular: objectstoreprofile
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ObjectStoreProfile is an administrator-managed object-store connection.
          It is cluster-scoped so tenants cannot create a profile that points the
          controller at an arbitrary endpoint or Secret.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ObjectStoreProfileSpec contains administrator-owned connection and access
              policy shared by controller-backed Backup and Restore workflows. The bucket
              and optional prefix scope are kept with the credentials so tenant workflows
              cannot expand the IAM permission boundary.
            properties:
              allowStaticCredentialsInTenantNamespace:
                description: |-
                  AllowStaticCredentialsInTenantNamespace is an explicit administrator
                  acknowledgement that the transfer chart will materialize static S3
                  credentials in the tenant namespace for the duration of a transfer. Keep
                  static profiles single-tenant; use workload identity for shared profiles.
                type: boolean
              allowedNamespaces:
                description: |-
                  AllowedNamespaces restricts which tenant namespace may reference a static
                  credentials profile. Static credentials are deliberately single-tenant.
                items:
                  description: |-
                    ObjectStoreNamespace is a Kubernetes namespace name permitted by an
                    administrator-managed ObjectStoreProfile. A named scalar gives the CRD
                    OpenAPI schema an item-level pattern without an expensive CEL loop.
                  maxLength: 63
                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                  type: string
                maxItems: 256
                type: array
              backend:
                allOf:
                - enum:
                  - s3
                - enum:
                  - s3
                type: string
              bucket:
                description: Bucket is the only bucket a workflow may access through
                  this profile.
                maxLength: 63
                minLength: 1
                pattern: ^[A-Za-z0-9][A-Za-z0-9._-]*$
                type: string
              credentialsSecret:
                description: |-
                  CredentialsSecret is an optional administrator-owned Secret used by the
                  controller for S3 locks, manifests, and inventory checks. Static
                  transfer profiles must set it because the transfer Pod needs credentials;
                  workload identity profiles may omit it when the controller uses its own
                  ambient cloud identity. It is always read from the controller namespace.
                properties:
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              endpoint:
                maxLength: 2048
                type: string
              forcePathStyle:
                type: boolean
              prefix:
                description: |-
                  Prefix scopes all workflow object names to this prefix. An empty prefix
                  permits the profile bucket root. Values are slash-separated safe segments.
                  The 639-character limit leaves room for the controller-owned cluster and
                  namespace scope, a 253-character recovery-point name, and the completion
                  manifest suffix within S3's 1024-byte object-key limit.
                maxLength: 639
                pattern: ^$|^[A-Za-z0-9][A-Za-z0-9._-]*(/[A-Za-z0-9][A-Za-z0-9._-]*)*$
                type: string
              provider:
                maxLength: 63
                type: string
              region:
                maxLength: 63
                type: string
              serverSideEncryption:
                enum:
                - AES256
                - aws:kms
                type: string
              serviceAccountRefs:
                description: |-
                  ServiceAccountRefs explicitly binds workload identity to administrator-
                  provisioned ServiceAccounts. Every workflow namespace must have one
                  matching entry, including the live ServiceAccount UID.
                items:
                  description: |-
                    ObjectStoreServiceAccountReference binds one tenant namespace to an
                    administrator-preprovisioned ServiceAccount. The UID and identity
                    fingerprint prevent a tenant from replacing or mutating the account before
                    a workflow's first reconcile.
                  properties:
                    identityFingerprint:
                      description: |-
                        IdentityFingerprint is the administrator-observed hash of the
                        ServiceAccount metadata that affects workload identity. Requiring the
                        fingerprint in the profile closes the window where a tenant could mutate
                        IAM annotations or automount settings before the first workflow reconcile.
                      pattern: ^[a-f0-9]{64}$
                      type: string
                    name:
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                      type: string
                    namespace:
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    uid:
                      description: |-
                        The UID is intentionally required. An administrator can update the
                        profile when rotating an identity by creating a new ServiceAccount.
                      minLength: 1
                      type: string
                  required:
                  - identityFingerprint
                  - name
                  - namespace
                  - uid
                  type: object
                maxItems: 256
                type: array
              sseKmsKeyID:
                description: SSEKMSKeyID is administrator-owned and is only used with
                  aws:kms.
                maxLength: 2048
                type: string
            required:
            - backend
            - bucket
            type: object
            x-kubernetes-validations:
            - message: static credential profiles require credentialsSecret; workload
                identity profiles may use the controller's ambient credentials
              rule: (has(self.serviceAccountRefs) && size(self.serviceAccountRefs)
                > 0) || (has(self.credentialsSecret) && size(self.credentialsSecret.name)
                > 0)
            - message: object store endpoints must use HTTPS
              rule: '!has(self.endpoint) || size(self.endpoint) == 0 || self.endpoint.startsWith(''https://'')'
            - message: sseKmsKeyID requires serverSideEncryption aws:kms
              rule: '!has(self.sseKmsKeyID) || size(self.sseKmsKeyID) == 0 || (has(self.serverSideEncryption)
                && self.serverSideEncryption == ''aws:kms'')'
            - message: configure workload identity or explicitly approve static credentials
                in the tenant namespace
              rule: (has(self.serviceAccountRefs) && size(self.serviceAccountRefs)
                > 0) || (has(self.allowStaticCredentialsInTenantNamespace) && self.allowStaticCredentialsInTenantNamespace)
            - message: static credentials profiles must allow exactly one tenant namespace
              rule: (has(self.serviceAccountRefs) && size(self.serviceAccountRefs)
                > 0) || (has(self.allowedNamespaces) && size(self.allowedNamespaces)
                == 1)
            - message: tenant-namespace projection approval is only valid without
                workload identity
              rule: '!(has(self.serviceAccountRefs) && size(self.serviceAccountRefs)
                > 0) || !has(self.allowStaticCredentialsInTenantNamespace) || !self.allowStaticCredentialsInTenantNamespace'
            - message: allowedNamespaces is only valid for static credential profiles
              rule: '!(has(self.serviceAccountRefs) && size(self.serviceAccountRefs)
                > 0) || !has(self.allowedNamespaces) || size(self.allowedNamespaces)
                == 0'
        required:
        - spec
        type: object
    served: true
    storage: true
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: podmigrations.migrate.sealos.io
spec:
  group: migrate.sealos.io
  names:
    kind: PodMigration
    listKind: PodMigrationList
    plural: podmigrations
    shortNames:
    - pmig
    singular: podmigration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              PodMigrationSpec is a workload-aware migration. Workload and precopy
              controls are exclusive to this operation.
            properties:
              createdBy:
                type: string
              deleteExtraneous:
                type: boolean
              destinationNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              openebsLvmEnableShared:
                type: boolean
              precopyPasses:
                type: integer
              sessionNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              skipSourceUsageCheck:
                type: boolean
              sourceNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              sourceNode:
                type: string
              strategies:
                items:
                  type: string
                maxItems: 32
                type: array
              targetNode:
                type: string
              temporaryNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              toolImage:
                type: string
              verifyChecksum:
                type: boolean
              volumes:
                items:
                  description: |-
                    VolumeSpec is planning output required to resume a PVC transfer. It is an
                    API-owned type with only the fields needed by transfer workflows.
                  properties:
                    accessModes:
                      items:
                        type: string
                      type: array
                    capacity:
                      type: string
                    concurrentConsumers:
                      type: integer
                    destinationPVC:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    sourceCapacity:
                      type: string
                    sourcePV:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    sourcePVC:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    sourcePVCMetadata:
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          type: object
                        labels:
                          additionalProperties:
                            type: string
                          type: object
                        ownerReferences:
                          items:
                            description: |-
                              OwnerReference contains enough information to let you identify an owning
                              object. An owning object must be in the same namespace as the dependent, or
                              be cluster-scoped, so there is no namespace field.
                            properties:
                              apiVersion:
                                description: API version of the referent.
                                type: string
                              blockOwnerDeletion:
                                description: |-
                                  If true, AND if the owner has the "foregroundDeletion" finalizer, then
                                  the owner cannot be deleted from the key-value store until this
                                  reference is removed.
                                  See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion
                                  for how the garbage collector interacts with this field and enforces the foreground deletion.
                                  Defaults to false.
                                  To set this field, a user needs "delete" permission of the owner,
                                  otherwise 422 (Unprocessable Entity) will be returned.
                                type: boolean
                              controller:
                                description: If true, this reference points to the
                                  managing controller.
                                type: boolean
                              kind:
                                description: |-
                                  Kind of the referent.
                                  More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                                type: string
                              name:
                                description: |-
                                  Name of the referent.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
                                type: string
                              uid:
                                description: |-
                                  UID of the referent.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
                                type: string
                            required:
                            - apiVersion
                            - kind
                            - name
                            - uid
                            type: object
                            x-kubernetes-map-type: atomic
                          type: array
                      type: object
                    sourcePVCSpec:
                      description: |-
                        PersistentVolumeClaimSpec describes the common attributes of storage devices
                        and allows a Source for provider-specific attributes
                      properties:
                        accessModes:
                          description: |-
                            accessModes contains the desired access modes the volume should have.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        dataSource:
                          description: |-
                            dataSource field can be used to specify either:
                            * An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)
                            * An existing PVC (PersistentVolumeClaim)
                            If the provisioner or an external controller can support the specified data source,
                            it will create a new volume based on the contents of the specified data source.
                            When the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,
                            and dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.
                            If the namespace is specified, then dataSourceRef will not be copied to dataSource.
                          properties:
                            apiGroup:
                              description: |-
                                APIGroup is the group for the resource being referenced.
                                If APIGroup is not specified, the specified Kind must be in the core API group.
                                For any other third-party types, APIGroup is required.
                              type: string
                            kind:
                              description: Kind is the type of resource being referenced
                              type: string
                            name:
                              description: Name is the name of resource being referenced
                              type: string
                          required:
                          - kind
                          - name
                          type: object
                          x-kubernetes-map-type: atomic
                        dataSourceRef:
                          description: |-
                            dataSourceRef specifies the object from which to populate the volume with data, if a non-empty
                            volume is desired. This may be any object from a non-empty API group (non
                            core object) or a PersistentVolumeClaim object.
                            When this field is specified, volume binding will only succeed if the type of
                            the specified object matches some installed volume populator or dynamic
                            provisioner.
                            This field will replace the functionality of the dataSource field and as such
                            if both fields are non-empty, they must have the same value. For backwards
                            compatibility, when namespace isn't specified in dataSourceRef,
                            both fields (dataSource and dataSourceRef) will be set to the same
                            value automatically if one of them is empty and the other is non-empty.
                            When namespace is specified in dataSourceRef,
                            dataSource isn't set to the same value and must be empty.
                            There are three important differences between dataSource and dataSourceRef:
                            * While dataSource only allows two specific types of objects, dataSourceRef
                              allows any non-core object, as well as PersistentVolumeClaim objects.
                            * While dataSource ignores disallowed values (dropping them), dataSourceRef
                              preserves all values, and generates an error if a disallowed value is
                              specified.
                            * While dataSource only allows local objects, dataSourceRef allows objects
                              in any namespaces.
                            (Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.
                            (Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                          properties:
                            apiGroup:
                              description: |-
                                APIGroup is the group for the resource being referenced.
                                If APIGroup is not specified, the specified Kind must be in the core API group.
                                For any other third-party types, APIGroup is required.
                              type: string
                            kind:
                              description: Kind is the type of resource being referenced
                              type: string
                            name:
                              description: Name is the name of resource being referenced
                              type: string
                            namespace:
                              description: |-
                                Namespace is the namespace of resource being referenced
                                Note that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.
                                (Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                              type: string
                          required:
                          - kind
                          - name
                          type: object
                        resources:
                          description: |-
                            resources represents the minimum resources the volume should have.
                            Users are allowed to specify resource requirements
                            that are lower than previous value but must still be higher than capacity recorded in the
                            status field of the claim.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources
                          properties:
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                        selector:
                          description: selector is a label query over volumes to consider
                            for binding.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        storageClassName:
                          description: |-
                            storageClassName is the name of the StorageClass required by the claim.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1
                          type: string
                        volumeAttributesClassName:
                          description: |-
                            volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.
                            If specified, the CSI driver will create or update the volume with the attributes defined
                            in the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,
                            it can be changed after the claim is created. An empty string or nil value indicates that no
                            VolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,
                            this field can be reset to its previous value (including nil) to cancel the modification.
                            If the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be
                            set to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource
                            exists.
                            More info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/
                          type: string
                        volumeMode:
                          description: |-
                            volumeMode defines what type of volume is required by the claim.
                            Value of Filesystem is implied when not included in claim spec.
                          type: string
                        volumeName:
                          description: volumeName is the binding reference to the
                            PersistentVolume backing this claim.
                          type: string
                      type: object
                    sourceReclaimPolicy:
                      description: PersistentVolumeReclaimPolicy describes a policy
                        for end-of-life maintenance of persistent volumes.
                      type: string
                    sourceUsageKnown:
                      type: boolean
                    sourceUsedBytes:
                      format: int64
                      type: integer
                    storageClass:
                      type: string
                    transferScope:
                      properties:
                        destinationPath:
                          maxLength: 1024
                          type: string
                        sourcePath:
                          maxLength: 1024
                          type: string
                      required:
                      - destinationPath
                      - sourcePath
                      type: object
                    volumeMode:
                      description: PersistentVolumeMode describes how a volume is
                        intended to be consumed, either Block or Filesystem.
                      type: string
                  required:
                  - accessModes
                  - capacity
                  - destinationPVC
                  - sourceCapacity
                  - sourcePV
                  - sourcePVC
                  - storageClass
                  - volumeMode
                  type: object
                maxItems: 1024
                type: array
              workload:
                description: |-
                  WorkloadSpec is specific to PodMigration and records workload ownership
                  and restoration data needed by that operation.
                properties:
                  adapter:
                    enum:
                    - None
                    - StandalonePod
                    - Deployment
                    - StatefulSet
                    - VictoriaLogs
                    - KubeBlocks
                    - VMCluster
                    - Grafana
                    type: string
                  affectedPods:
                    items:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    maxItems: 1024
                    type: array
                  controller:
                    description: ObjectReference identifies a Kubernetes object involved
                      in a workflow.
                    properties:
                      apiVersion:
                        maxLength: 253
                        type: string
                      kind:
                        maxLength: 253
                        type: string
                      name:
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                        type: string
                      namespace:
                        maxLength: 63
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                      resourceVersion:
                        maxLength: 256
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    type: object
                  grafana:
                    properties:
                      apiVersion:
                        type: string
                      name:
                        type: string
                      originalReplicas:
                        format: int32
                        type: integer
                      originalSuspend:
                        type: boolean
                      originalSuspendConfigured:
                        type: boolean
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - apiVersion
                    - name
                    - originalReplicas
                    - originalSuspend
                    - originalSuspendConfigured
                    type: object
                  kubeBlocks:
                    properties:
                      cluster:
                        type: string
                      clusterUID:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                      component:
                        type: string
                      instance:
                        type: string
                      opsAPIVersion:
                        type: string
                      originalPaused:
                        type: boolean
                      originalPausedConfigured:
                        type: boolean
                      role:
                        type: string
                      switchoverCandidate:
                        type: string
                      switchoverContainer:
                        type: string
                      switchoverStrategy:
                        type: string
                    required:
                    - cluster
                    - clusterUID
                    - component
                    - instance
                    - opsAPIVersion
                    type: object
                  ordinal:
                    format: int32
                    type: integer
                  originalObject:
                    description: |-
                      The serialized byte-size limit is enforced by the controller/domain
                      boundary because OpenAPI cannot express a byte limit for opaque JSON.
                    x-kubernetes-preserve-unknown-fields: true
                  originalReplicas:
                    format: int32
                    type: integer
                  pod:
                    description: ObjectReference identifies a Kubernetes object involved
                      in a workflow.
                    properties:
                      apiVersion:
                        maxLength: 253
                        type: string
                      kind:
                        maxLength: 253
                        type: string
                      name:
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                        type: string
                      namespace:
                        maxLength: 63
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                      resourceVersion:
                        maxLength: 256
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    type: object
                  vmCluster:
                    properties:
                      apiVersion:
                        type: string
                      component:
                        type: string
                      name:
                        type: string
                      originalClusterPaused:
                        type: boolean
                      originalClusterPausedConfigured:
                        type: boolean
                      originalPaused:
                        type: boolean
                      originalPausedConfigured:
                        type: boolean
                      originalReplicas:
                        format: int32
                        type: integer
                      originalReplicasConfigured:
                        type: boolean
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - apiVersion
                    - component
                    - name
                    - originalClusterPaused
                    - originalClusterPausedConfigured
                    - originalPaused
                    - originalPausedConfigured
                    - originalReplicas
                    - originalReplicasConfigured
                    type: object
                required:
                - adapter
                type: object
            required:
            - destinationNamespace
            - precopyPasses
            - sessionNamespace
            - sourceNamespace
            - temporaryNamespace
            - workload
            type: object
            x-kubernetes-validations:
            - message: workflow namespaces must match metadata.namespace
              rule: self.sourceNamespace == self.sessionNamespace && self.destinationNamespace
                == self.sessionNamespace && self.temporaryNamespace == self.sessionNamespace
            - message: PodMigration workload.adapter must identify a supported workload
              rule: self.workload.adapter != 'None'
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 8192
                      type: string
                    reason:
                      maxLength: 128
                      type: string
                    status:
                      type: string
                    type:
                      maxLength: 64
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
              failureReason:
                maxLength: 8192
                type: string
              history:
                items:
                  properties:
                    message:
                      maxLength: 8192
                      type: string
                    phase:
                      description: WorkflowPhase identifies one durable workflow state.
                      enum:
                      - Planned
                      - Reserving
                      - Reserved
                      - WarmCopying
                      - WarmCopied
                      - Pausing
                      - Paused
                      - FinalSyncing
                      - FinalSynced
                      - Activating
                      - Activated
                      - Resuming
                      - Completed
                      - Aborting
                      - Aborted
                      - RollingBack
                      - RolledBack
                      - Renaming
                      - Moving
                      - Failed
                      type: string
                    time:
                      format: date-time
                      type: string
                  required:
                  - phase
                  - time
                  type: object
                maxItems: 256
                type: array
              message:
                maxLength: 8192
                type: string
              observedGeneration:
                format: int64
                type: integer
              openebsLvmSharedMounts:
                items:
                  properties:
                    lvmVolume:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    previousShared:
                      type: string
                    previousSharedSet:
                      type: boolean
                    sourcePV:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                  required:
                  - lvmVolume
                  - sourcePV
                  type: object
                type: array
              originalPodSnapshotHash:
                description: |-
                  OriginalPodSnapshotHash is controller-owned evidence that the standalone
                  Pod snapshot was captured from the referenced live Pod before execution.
                type: string
              phase:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              resumeFrom:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              startedAt:
                format: date-time
                type: string
              updatedAt:
                format: date-time
                type: string
              volumes:
                items:
                  description: |-
                    PodMigrationVolumeStatus is the durable checkpoint for a workload-aware
                    migration volume. It is a distinct API type even though its transfer and
                    activation fields currently match MigrationVolumeStatus.
                  properties:
                    activation:
                      properties:
                        activatedAt:
                          format: date-time
                          type: string
                        activePVC:
                          description: ObjectReference identifies a Kubernetes object
                            involved in a workflow.
                          properties:
                            apiVersion:
                              maxLength: 253
                              type: string
                            kind:
                              maxLength: 253
                              type: string
                            name:
                              maxLength: 253
                              minLength: 1
                              pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                              type: string
                            namespace:
                              maxLength: 63
                              pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                              type: string
                            resourceVersion:
                              maxLength: 256
                              type: string
                            uid:
                              description: |-
                                UID is a type that holds unique ID values, including UUIDs.  Because we
                                don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                                intent and helps make sure that UIDs and names do not get conflated.
                              type: string
                          required:
                          - name
                          type: object
                        destinationReserved:
                          type: boolean
                        rolledBackAt:
                          format: date-time
                          type: string
                        sourcePVCDeleted:
                          type: boolean
                        temporaryPVCDeleted:
                          type: boolean
                      type: object
                    destinationPV:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    destinationPVC:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    destinationReclaimPolicy:
                      description: PersistentVolumeReclaimPolicy describes a policy
                        for end-of-life maintenance of persistent volumes.
                      type: string
                    reserved:
                      type: boolean
                    sourcePVCName:
                      type: string
                    sync:
                      description: PodMigrationSyncStatus tracks both warm-copy and
                        final-copy checkpoints.
                      properties:
                        attempts:
                          type: integer
                        bytesCopied:
                          format: int64
                          type: integer
                        checksumVerified:
                          type: boolean
                        finalCompletedAt:
                          format: date-time
                          type: string
                        lastError:
                          maxLength: 8192
                          type: string
                        warmCompletedAt:
                          format: date-time
                          type: string
                      required:
                      - attempts
                      type: object
                  required:
                  - activation
                  - sourcePVCName
                  - sync
                  type: object
                type: array
              warmPassesCompleted:
                type: integer
              workload:
                description: |-
                  PodMigrationWorkloadStatus tracks Pod identities recreated while pausing,
                  resuming, or rolling back a workload. The original migration target and
                  recovery snapshot remain immutable in spec.workload.
                properties:
                  affectedPods:
                    items:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  pod:
                    description: ObjectReference identifies a Kubernetes object involved
                      in a workflow.
                    properties:
                      apiVersion:
                        maxLength: 253
                        type: string
                      kind:
                        maxLength: 253
                        type: string
                      name:
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                        type: string
                      namespace:
                        maxLength: 63
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        type: string
                      resourceVersion:
                        maxLength: 256
                        type: string
                      uid:
                        description: |-
                          UID is a type that holds unique ID values, including UUIDs.  Because we
                          don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                          intent and helps make sure that UIDs and names do not get conflated.
                        type: string
                    required:
                    - name
                    type: object
                type: object
            required:
            - phase
            - startedAt
            - updatedAt
            - volumes
            - warmPassesCompleted
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: renames.migrate.sealos.io
spec:
  group: migrate.sealos.io
  names:
    kind: Rename
    listKind: RenameList
    plural: renames
    shortNames:
    - prename
    singular: rename
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              createdBy:
                type: string
              destinationPVC:
                description: ObjectReference identifies a Kubernetes object involved
                  in a workflow.
                properties:
                  apiVersion:
                    maxLength: 253
                    type: string
                  kind:
                    maxLength: 253
                    type: string
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                  namespace:
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  resourceVersion:
                    maxLength: 256
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                type: object
              sessionNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              sourceNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              sourcePV:
                description: ObjectReference identifies a Kubernetes object involved
                  in a workflow.
                properties:
                  apiVersion:
                    maxLength: 253
                    type: string
                  kind:
                    maxLength: 253
                    type: string
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                  namespace:
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  resourceVersion:
                    maxLength: 256
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                type: object
              sourcePVC:
                description: ObjectReference identifies a Kubernetes object involved
                  in a workflow.
                properties:
                  apiVersion:
                    maxLength: 253
                    type: string
                  kind:
                    maxLength: 253
                    type: string
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                  namespace:
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  resourceVersion:
                    maxLength: 256
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                type: object
              sourceTemplate:
                properties:
                  metadata:
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                      ownerReferences:
                        items:
                          description: |-
                            OwnerReference contains enough information to let you identify an owning
                            object. An owning object must be in the same namespace as the dependent, or
                            be cluster-scoped, so there is no namespace field.
                          properties:
                            apiVersion:
                              description: API version of the referent.
                              type: string
                            blockOwnerDeletion:
                              description: |-
                                If true, AND if the owner has the "foregroundDeletion" finalizer, then
                                the owner cannot be deleted from the key-value store until this
                                reference is removed.
                                See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion
                                for how the garbage collector interacts with this field and enforces the foreground deletion.
                                Defaults to false.
                                To set this field, a user needs "delete" permission of the owner,
                                otherwise 422 (Unprocessable Entity) will be returned.
                              type: boolean
                            controller:
                              description: If true, this reference points to the managing
                                controller.
                              type: boolean
                            kind:
                              description: |-
                                Kind of the referent.
                                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                              type: string
                            name:
                              description: |-
                                Name of the referent.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
                              type: string
                            uid:
                              description: |-
                                UID of the referent.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
                              type: string
                          required:
                          - apiVersion
                          - kind
                          - name
                          - uid
                          type: object
                          x-kubernetes-map-type: atomic
                        type: array
                    type: object
                  reclaimPolicy:
                    description: PersistentVolumeReclaimPolicy describes a policy
                      for end-of-life maintenance of persistent volumes.
                    type: string
                  spec:
                    description: |-
                      PersistentVolumeClaimSpec describes the common attributes of storage devices
                      and allows a Source for provider-specific attributes
                    properties:
                      accessModes:
                        description: |-
                          accessModes contains the desired access modes the volume should have.
                          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: atomic
                      dataSource:
                        description: |-
                          dataSource field can be used to specify either:
                          * An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)
                          * An existing PVC (PersistentVolumeClaim)
                          If the provisioner or an external controller can support the specified data source,
                          it will create a new volume based on the contents of the specified data source.
                          When the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,
                          and dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.
                          If the namespace is specified, then dataSourceRef will not be copied to dataSource.
                        properties:
                          apiGroup:
                            description: |-
                              APIGroup is the group for the resource being referenced.
                              If APIGroup is not specified, the specified Kind must be in the core API group.
                              For any other third-party types, APIGroup is required.
                            type: string
                          kind:
                            description: Kind is the type of resource being referenced
                            type: string
                          name:
                            description: Name is the name of resource being referenced
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                        x-kubernetes-map-type: atomic
                      dataSourceRef:
                        description: |-
                          dataSourceRef specifies the object from which to populate the volume with data, if a non-empty
                          volume is desired. This may be any object from a non-empty API group (non
                          core object) or a PersistentVolumeClaim object.
                          When this field is specified, volume binding will only succeed if the type of
                          the specified object matches some installed volume populator or dynamic
                          provisioner.
                          This field will replace the functionality of the dataSource field and as such
                          if both fields are non-empty, they must have the same value. For backwards
                          compatibility, when namespace isn't specified in dataSourceRef,
                          both fields (dataSource and dataSourceRef) will be set to the same
                          value automatically if one of them is empty and the other is non-empty.
                          When namespace is specified in dataSourceRef,
                          dataSource isn't set to the same value and must be empty.
                          There are three important differences between dataSource and dataSourceRef:
                          * While dataSource only allows two specific types of objects, dataSourceRef
                            allows any non-core object, as well as PersistentVolumeClaim objects.
                          * While dataSource ignores disallowed values (dropping them), dataSourceRef
                            preserves all values, and generates an error if a disallowed value is
                            specified.
                          * While dataSource only allows local objects, dataSourceRef allows objects
                            in any namespaces.
                          (Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.
                          (Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                        properties:
                          apiGroup:
                            description: |-
                              APIGroup is the group for the resource being referenced.
                              If APIGroup is not specified, the specified Kind must be in the core API group.
                              For any other third-party types, APIGroup is required.
                            type: string
                          kind:
                            description: Kind is the type of resource being referenced
                            type: string
                          name:
                            description: Name is the name of resource being referenced
                            type: string
                          namespace:
                            description: |-
                              Namespace is the namespace of resource being referenced
                              Note that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.
                              (Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                            type: string
                        required:
                        - kind
                        - name
                        type: object
                      resources:
                        description: |-
                          resources represents the minimum resources the volume should have.
                          Users are allowed to specify resource requirements
                          that are lower than previous value but must still be higher than capacity recorded in the
                          status field of the claim.
                          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources
                        properties:
                          limits:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Limits describes the maximum amount of compute resources allowed.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                          requests:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: |-
                              Requests describes the minimum amount of compute resources required.
                              If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                              otherwise to an implementation-defined value. Requests cannot exceed Limits.
                              More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                            type: object
                        type: object
                      selector:
                        description: selector is a label query over volumes to consider
                          for binding.
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                      storageClassName:
                        description: |-
                          storageClassName is the name of the StorageClass required by the claim.
                          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1
                        type: string
                      volumeAttributesClassName:
                        description: |-
                          volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.
                          If specified, the CSI driver will create or update the volume with the attributes defined
                          in the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,
                          it can be changed after the claim is created. An empty string or nil value indicates that no
                          VolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,
                          this field can be reset to its previous value (including nil) to cancel the modification.
                          If the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be
                          set to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource
                          exists.
                          More info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/
                        type: string
                      volumeMode:
                        description: |-
                          volumeMode defines what type of volume is required by the claim.
                          Value of Filesystem is implied when not included in claim spec.
                        type: string
                      volumeName:
                        description: volumeName is the binding reference to the PersistentVolume
                          backing this claim.
                        type: string
                    type: object
                required:
                - spec
                type: object
            required:
            - destinationPVC
            - sessionNamespace
            - sourceNamespace
            - sourcePV
            - sourcePVC
            - sourceTemplate
            type: object
            x-kubernetes-validations:
            - message: workflow namespace must match metadata.namespace
              rule: self.sourceNamespace == self.sessionNamespace
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 8192
                      type: string
                    reason:
                      maxLength: 128
                      type: string
                    status:
                      type: string
                    type:
                      maxLength: 64
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
              failureReason:
                maxLength: 8192
                type: string
              history:
                items:
                  properties:
                    message:
                      maxLength: 8192
                      type: string
                    phase:
                      description: WorkflowPhase identifies one durable workflow state.
                      enum:
                      - Planned
                      - Reserving
                      - Reserved
                      - WarmCopying
                      - WarmCopied
                      - Pausing
                      - Paused
                      - FinalSyncing
                      - FinalSynced
                      - Activating
                      - Activated
                      - Resuming
                      - Completed
                      - Aborting
                      - Aborted
                      - RollingBack
                      - RolledBack
                      - Renaming
                      - Moving
                      - Failed
                      type: string
                    time:
                      format: date-time
                      type: string
                  required:
                  - phase
                  - time
                  type: object
                maxItems: 256
                type: array
              message:
                maxLength: 8192
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              resumeFrom:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              startedAt:
                format: date-time
                type: string
              updatedAt:
                format: date-time
                type: string
              volumes:
                items:
                  description: |-
                    PVCIdentityVolumeStatus is the checkpoint for Rename and Move. Those
                    operations change PVC identity and never run a data-copy phase.
                  properties:
                    activation:
                      description: |-
                        PVCIdentityActivationStatus is the checkpoint needed to roll back a
                        Rename or Move. Temporary-volume cleanup fields do not apply to identity
                        operations.
                      properties:
                        activatedAt:
                          format: date-time
                          type: string
                        activePVC:
                          description: ObjectReference identifies a Kubernetes object
                            involved in a workflow.
                          properties:
                            apiVersion:
                              maxLength: 253
                              type: string
                            kind:
                              maxLength: 253
                              type: string
                            name:
                              maxLength: 253
                              minLength: 1
                              pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                              type: string
                            namespace:
                              maxLength: 63
                              pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                              type: string
                            resourceVersion:
                              maxLength: 256
                              type: string
                            uid:
                              description: |-
                                UID is a type that holds unique ID values, including UUIDs.  Because we
                                don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                                intent and helps make sure that UIDs and names do not get conflated.
                              type: string
                          required:
                          - name
                          type: object
                        rolledBackAt:
                          format: date-time
                          type: string
                      type: object
                    sourcePVCName:
                      type: string
                  required:
                  - activation
                  - sourcePVCName
                  type: object
                type: array
            required:
            - phase
            - startedAt
            - updatedAt
            - volumes
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: reservations.migrate.sealos.io
spec:
  group: migrate.sealos.io
  names:
    kind: Reservation
    listKind: ReservationList
    plural: reservations
    shortNames:
    - resv
    singular: reservation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              createdBy:
                type: string
              destinationNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              sessionNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              skipSourceUsageCheck:
                type: boolean
              sourceNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              targetNode:
                type: string
              temporaryNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              toolImage:
                type: string
              volumes:
                items:
                  description: |-
                    VolumeSpec is planning output required to resume a PVC transfer. It is an
                    API-owned type with only the fields needed by transfer workflows.
                  properties:
                    accessModes:
                      items:
                        type: string
                      type: array
                    capacity:
                      type: string
                    concurrentConsumers:
                      type: integer
                    destinationPVC:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    sourceCapacity:
                      type: string
                    sourcePV:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    sourcePVC:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    sourcePVCMetadata:
                      properties:
                        annotations:
                          additionalProperties:
                            type: string
                          type: object
                        labels:
                          additionalProperties:
                            type: string
                          type: object
                        ownerReferences:
                          items:
                            description: |-
                              OwnerReference contains enough information to let you identify an owning
                              object. An owning object must be in the same namespace as the dependent, or
                              be cluster-scoped, so there is no namespace field.
                            properties:
                              apiVersion:
                                description: API version of the referent.
                                type: string
                              blockOwnerDeletion:
                                description: |-
                                  If true, AND if the owner has the "foregroundDeletion" finalizer, then
                                  the owner cannot be deleted from the key-value store until this
                                  reference is removed.
                                  See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion
                                  for how the garbage collector interacts with this field and enforces the foreground deletion.
                                  Defaults to false.
                                  To set this field, a user needs "delete" permission of the owner,
                                  otherwise 422 (Unprocessable Entity) will be returned.
                                type: boolean
                              controller:
                                description: If true, this reference points to the
                                  managing controller.
                                type: boolean
                              kind:
                                description: |-
                                  Kind of the referent.
                                  More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                                type: string
                              name:
                                description: |-
                                  Name of the referent.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
                                type: string
                              uid:
                                description: |-
                                  UID of the referent.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
                                type: string
                            required:
                            - apiVersion
                            - kind
                            - name
                            - uid
                            type: object
                            x-kubernetes-map-type: atomic
                          type: array
                      type: object
                    sourcePVCSpec:
                      description: |-
                        PersistentVolumeClaimSpec describes the common attributes of storage devices
                        and allows a Source for provider-specific attributes
                      properties:
                        accessModes:
                          description: |-
                            accessModes contains the desired access modes the volume should have.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                        dataSource:
                          description: |-
                            dataSource field can be used to specify either:
                            * An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)
                            * An existing PVC (PersistentVolumeClaim)
                            If the provisioner or an external controller can support the specified data source,
                            it will create a new volume based on the contents of the specified data source.
                            When the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,
                            and dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.
                            If the namespace is specified, then dataSourceRef will not be copied to dataSource.
                          properties:
                            apiGroup:
                              description: |-
                                APIGroup is the group for the resource being referenced.
                                If APIGroup is not specified, the specified Kind must be in the core API group.
                                For any other third-party types, APIGroup is required.
                              type: string
                            kind:
                              description: Kind is the type of resource being referenced
                              type: string
                            name:
                              description: Name is the name of resource being referenced
                              type: string
                          required:
                          - kind
                          - name
                          type: object
                          x-kubernetes-map-type: atomic
                        dataSourceRef:
                          description: |-
                            dataSourceRef specifies the object from which to populate the volume with data, if a non-empty
                            volume is desired. This may be any object from a non-empty API group (non
                            core object) or a PersistentVolumeClaim object.
                            When this field is specified, volume binding will only succeed if the type of
                            the specified object matches some installed volume populator or dynamic
                            provisioner.
                            This field will replace the functionality of the dataSource field and as such
                            if both fields are non-empty, they must have the same value. For backwards
                            compatibility, when namespace isn't specified in dataSourceRef,
                            both fields (dataSource and dataSourceRef) will be set to the same
                            value automatically if one of them is empty and the other is non-empty.
                            When namespace is specified in dataSourceRef,
                            dataSource isn't set to the same value and must be empty.
                            There are three important differences between dataSource and dataSourceRef:
                            * While dataSource only allows two specific types of objects, dataSourceRef
                              allows any non-core object, as well as PersistentVolumeClaim objects.
                            * While dataSource ignores disallowed values (dropping them), dataSourceRef
                              preserves all values, and generates an error if a disallowed value is
                              specified.
                            * While dataSource only allows local objects, dataSourceRef allows objects
                              in any namespaces.
                            (Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.
                            (Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                          properties:
                            apiGroup:
                              description: |-
                                APIGroup is the group for the resource being referenced.
                                If APIGroup is not specified, the specified Kind must be in the core API group.
                                For any other third-party types, APIGroup is required.
                              type: string
                            kind:
                              description: Kind is the type of resource being referenced
                              type: string
                            name:
                              description: Name is the name of resource being referenced
                              type: string
                            namespace:
                              description: |-
                                Namespace is the namespace of resource being referenced
                                Note that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.
                                (Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
                              type: string
                          required:
                          - kind
                          - name
                          type: object
                        resources:
                          description: |-
                            resources represents the minimum resources the volume should have.
                            Users are allowed to specify resource requirements
                            that are lower than previous value but must still be higher than capacity recorded in the
                            status field of the claim.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources
                          properties:
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                        selector:
                          description: selector is a label query over volumes to consider
                            for binding.
                          properties:
                            matchExpressions:
                              description: matchExpressions is a list of label selector
                                requirements. The requirements are ANDed.
                              items:
                                description: |-
                                  A label selector requirement is a selector that contains values, a key, and an operator that
                                  relates the key and values.
                                properties:
                                  key:
                                    description: key is the label key that the selector
                                      applies to.
                                    type: string
                                  operator:
                                    description: |-
                                      operator represents a key's relationship to a set of values.
                                      Valid operators are In, NotIn, Exists and DoesNotExist.
                                    type: string
                                  values:
                                    description: |-
                                      values is an array of string values. If the operator is In or NotIn,
                                      the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                      the values array must be empty. This array is replaced during a strategic
                                      merge patch.
                                    items:
                                      type: string
                                    type: array
                                    x-kubernetes-list-type: atomic
                                required:
                                - key
                                - operator
                                type: object
                              type: array
                              x-kubernetes-list-type: atomic
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: |-
                                matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                map is equivalent to an element of matchExpressions, whose key field is "key", the
                                operator is "In", and the values array contains only "value". The requirements are ANDed.
                              type: object
                          type: object
                          x-kubernetes-map-type: atomic
                        storageClassName:
                          description: |-
                            storageClassName is the name of the StorageClass required by the claim.
                            More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1
                          type: string
                        volumeAttributesClassName:
                          description: |-
                            volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.
                            If specified, the CSI driver will create or update the volume with the attributes defined
                            in the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,
                            it can be changed after the claim is created. An empty string or nil value indicates that no
                            VolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,
                            this field can be reset to its previous value (including nil) to cancel the modification.
                            If the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be
                            set to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource
                            exists.
                            More info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/
                          type: string
                        volumeMode:
                          description: |-
                            volumeMode defines what type of volume is required by the claim.
                            Value of Filesystem is implied when not included in claim spec.
                          type: string
                        volumeName:
                          description: volumeName is the binding reference to the
                            PersistentVolume backing this claim.
                          type: string
                      type: object
                    sourceReclaimPolicy:
                      description: PersistentVolumeReclaimPolicy describes a policy
                        for end-of-life maintenance of persistent volumes.
                      type: string
                    sourceUsageKnown:
                      type: boolean
                    sourceUsedBytes:
                      format: int64
                      type: integer
                    storageClass:
                      type: string
                    transferScope:
                      properties:
                        destinationPath:
                          maxLength: 1024
                          type: string
                        sourcePath:
                          maxLength: 1024
                          type: string
                      required:
                      - destinationPath
                      - sourcePath
                      type: object
                    volumeMode:
                      description: PersistentVolumeMode describes how a volume is
                        intended to be consumed, either Block or Filesystem.
                      type: string
                  required:
                  - accessModes
                  - capacity
                  - destinationPVC
                  - sourceCapacity
                  - sourcePV
                  - sourcePVC
                  - storageClass
                  - volumeMode
                  type: object
                maxItems: 1024
                type: array
            required:
            - destinationNamespace
            - sessionNamespace
            - sourceNamespace
            - temporaryNamespace
            type: object
            x-kubernetes-validations:
            - message: workflow namespaces must match metadata.namespace
              rule: self.sourceNamespace == self.sessionNamespace && self.destinationNamespace
                == self.sessionNamespace && self.temporaryNamespace == self.sessionNamespace
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 8192
                      type: string
                    reason:
                      maxLength: 128
                      type: string
                    status:
                      type: string
                    type:
                      maxLength: 64
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
              failureReason:
                maxLength: 8192
                type: string
              history:
                items:
                  properties:
                    message:
                      maxLength: 8192
                      type: string
                    phase:
                      description: WorkflowPhase identifies one durable workflow state.
                      enum:
                      - Planned
                      - Reserving
                      - Reserved
                      - WarmCopying
                      - WarmCopied
                      - Pausing
                      - Paused
                      - FinalSyncing
                      - FinalSynced
                      - Activating
                      - Activated
                      - Resuming
                      - Completed
                      - Aborting
                      - Aborted
                      - RollingBack
                      - RolledBack
                      - Renaming
                      - Moving
                      - Failed
                      type: string
                    time:
                      format: date-time
                      type: string
                  required:
                  - phase
                  - time
                  type: object
                maxItems: 256
                type: array
              message:
                maxLength: 8192
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              resumeFrom:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              startedAt:
                format: date-time
                type: string
              updatedAt:
                format: date-time
                type: string
              volumes:
                items:
                  description: |-
                    ReservationVolumeStatus reports only destination reservation progress.
                    Copying and activation checkpoints are not part of a Reservation contract.
                  properties:
                    destinationPV:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    destinationPVC:
                      description: ObjectReference identifies a Kubernetes object
                        involved in a workflow.
                      properties:
                        apiVersion:
                          maxLength: 253
                          type: string
                        kind:
                          maxLength: 253
                          type: string
                        name:
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                          type: string
                        namespace:
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        resourceVersion:
                          maxLength: 256
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    destinationReclaimPolicy:
                      description: PersistentVolumeReclaimPolicy describes a policy
                        for end-of-life maintenance of persistent volumes.
                      type: string
                    reserved:
                      type: boolean
                    sourcePVCName:
                      type: string
                  required:
                  - sourcePVCName
                  type: object
                type: array
            required:
            - phase
            - startedAt
            - updatedAt
            - volumes
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: restores.migrate.sealos.io
spec:
  group: migrate.sealos.io
  names:
    kind: Restore
    listKind: RestoreList
    plural: restores
    shortNames:
    - rest
    singular: restore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              allowMounted:
                type: boolean
              createPVC:
                type: boolean
              createdBy:
                type: string
              deleteExtraneous:
                type: boolean
              destinationAccessMode:
                type: string
              destinationCapacity:
                type: string
              destinationNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              destinationPVC:
                description: ObjectReference identifies a Kubernetes object involved
                  in a workflow.
                properties:
                  apiVersion:
                    maxLength: 253
                    type: string
                  kind:
                    maxLength: 253
                    type: string
                  name:
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                    type: string
                  namespace:
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  resourceVersion:
                    maxLength: 256
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                type: object
              destinationStorageClass:
                type: string
              name:
                maxLength: 253
                minLength: 1
                pattern: ^[A-Za-z0-9][A-Za-z0-9._-]*$
                type: string
              objectStoreProfile:
                description: |-
                  ObjectStoreProfile selects an administrator-owned cluster profile. The
                  profile supplies endpoint, bucket scope, and credentials.
                maxLength: 253
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                type: string
              path:
                type: string
              sessionNamespace:
                maxLength: 63
                minLength: 1
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
              targetNode:
                type: string
              toolImage:
                type: string
            required:
            - destinationNamespace
            - destinationPVC
            - name
            - objectStoreProfile
            - sessionNamespace
            type: object
            x-kubernetes-validations:
            - message: workflow namespace must match metadata.namespace
              rule: self.destinationNamespace == self.sessionNamespace
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 8192
                      type: string
                    reason:
                      maxLength: 128
                      type: string
                    status:
                      type: string
                    type:
                      maxLength: 64
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                maxItems: 32
                type: array
              failureReason:
                maxLength: 8192
                type: string
              history:
                items:
                  properties:
                    message:
                      maxLength: 8192
                      type: string
                    phase:
                      description: WorkflowPhase identifies one durable workflow state.
                      enum:
                      - Planned
                      - Reserving
                      - Reserved
                      - WarmCopying
                      - WarmCopied
                      - Pausing
                      - Paused
                      - FinalSyncing
                      - FinalSynced
                      - Activating
                      - Activated
                      - Resuming
                      - Completed
                      - Aborting
                      - Aborted
                      - RollingBack
                      - RolledBack
                      - Renaming
                      - Moving
                      - Failed
                      type: string
                    time:
                      format: date-time
                      type: string
                  required:
                  - phase
                  - time
                  type: object
                maxItems: 256
                type: array
              message:
                maxLength: 8192
                type: string
              objectStoreCredentialsSecretUID:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
              objectStoreProfileGeneration:
                format: int64
                type: integer
              objectStoreProfileUID:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
              objectStoreServiceAccountFingerprint:
                type: string
              objectStoreServiceAccountUID:
                description: |-
                  UID is a type that holds unique ID values, including UUIDs.  Because we
                  don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                  intent and helps make sure that UIDs and names do not get conflated.
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              resumeFrom:
                description: WorkflowPhase identifies one durable workflow state.
                enum:
                - Planned
                - Reserving
                - Reserved
                - WarmCopying
                - WarmCopied
                - Pausing
                - Paused
                - FinalSyncing
                - FinalSynced
                - Activating
                - Activated
                - Resuming
                - Completed
                - Aborting
                - Aborted
                - RollingBack
                - RolledBack
                - Renaming
                - Moving
                - Failed
                type: string
              startedAt:
                format: date-time
                type: string
              updatedAt:
                format: date-time
                type: string
            required:
            - phase
            - startedAt
            - updatedAt
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
